/*
 * Configuração pública do dashboard.
 * Nunca coloque service_role, client_secret ou chaves privadas neste arquivo.
 * A anonKey do Supabase pode ficar no cliente porque o acesso é protegido por RLS.
 */
window.C18_CONFIG = {
  mode: "demo",
  supabaseUrl: "",
  supabaseAnonKey: "",
  lowStockThreshold: 3,
};
